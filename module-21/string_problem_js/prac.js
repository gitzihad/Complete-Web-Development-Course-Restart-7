
let myObject = {
    property1: "value1",
    property2: "value2",
    changeableProperty: "initialValue"
};

// Changing the value of the changeable property
myObject.changeableProperty = "newValue";

// Logging the object to see the changes
console.log(myObject);
